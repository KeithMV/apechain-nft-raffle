{
  "name": "@apechain/raffle-contracts",
  "version": "1.0.0",
  "scripts": {
    "compile": "yarn hardhat compile",
    "test": "yarn hardhat test",
    "test:coverage": "yarn hardhat coverage",
    "deploy": "yarn hardhat run scripts/deploy.js --network apechain",
    "deploy-secure": "yarn hardhat run scripts/deploy-secure.js --network apechain",
    "deploy-secure-v2": "yarn hardhat run scripts/deploy-secure-v2.js --network apechain",
    "verify-deployment": "yarn hardhat run scripts/verify-secure-deployment.js --network apechain",
    "security-check": "yarn hardhat run scripts/security-comparison.js",
    "security-validate-v2": "yarn hardhat run scripts/security-validation-v2.js --network apechain",
    "verify": "yarn hardhat verify --network apechain",
    "fee:status": "yarn hardhat run scripts/fee-management.js --network apechain -- status",
    "fee:promotional": "yarn hardhat run scripts/fee-management.js --network apechain -- promotional",
    "fee:competitive": "yarn hardhat run scripts/fee-management.js --network apechain -- competitive",
    "fee:standard": "yarn hardhat run scripts/fee-management.js --network apechain -- standard",
    "fee:premium": "yarn hardhat run scripts/fee-management.js --network apechain -- premium",
    "fee:help": "yarn hardhat run scripts/fee-management.js --network apechain -- --help"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-chai-matchers": "^2.1.2",
    "@nomicfoundation/hardhat-network-helpers": "^1.1.2",
    "@nomiclabs/hardhat-ethers": "^2.2.3",
    "chai": "^4.5.0",
    "hardhat": "^2.28.4"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^4.9.6",
    "aws-sdk": "^2.1692.0",
    "dotenv": "^16.4.5",
    "ethers": "5.7.2"
  }
}
